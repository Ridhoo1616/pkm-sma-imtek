# Folder ini berisi kode internal aplikasi dan tidak boleh diakses langsung
# melalui browser. Berlaku untuk Apache 2.4 (XAMPP) dan Apache 2.2.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
